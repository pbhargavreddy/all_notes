for loop backtracking recursion tree. 
-> It gives all subset of a given set. At every call set is a valid subset.

Code:

def backtrack(start, path):

    res.append(path.copy())

    for i in range(start, len(nums)):

        path.append(nums[i])   # choose

        backtrack(i+1, path)   # explore

        path.pop()             # undo
        

Tri:

[]
├── [1]
│   ├── [1,2]
│   │   └── [1,2,3]
│   └── [1,3]
├── [2]
│   └── [2,3]
└── [3]
